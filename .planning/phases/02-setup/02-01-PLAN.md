---
phase: 02-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup:
  - service: python-venv
    why: "Python virtual environment (built-in since Python 3.3)"
    dashboard_config:
      - task: "Python 3.11+ installed"
        location: "python --version"
  - service: docker
    why: "Container runtime for deployment"
    dashboard_config:
      - task: "Install Docker"
        location: "https://docs.docker.com/get-docker/"
      - task: "Install Docker Compose"
        location: "https://docs.docker.com/compose/install/"
  - service: ollama
    why: "LLM runtime for 1-bit models"
    dashboard_config:
      - task: "Install Ollama"
        location: "https://ollama.com/download"

must_haves:
  truths:
    - "Project structure exists with proper Python packaging"
    - "Dependencies install without conflicts"
    - "CLI runs and shows help"
    - "Docker builds and runs successfully"
    - "Docker Compose starts all services"
  artifacts:
    - path: "src/bitrag/__init__.py"
      provides: "Python package init"
    - path: "src/bitrag/cli/main.py"
      provides: "CLI entry point"
    - path: "requirements.txt"
      provides: "Python dependencies"
    - path: "pyproject.toml"
      provides: "Package configuration"
    - path: "docker/Dockerfile"
      provides: "Application container"
    - path: "docker/docker-compose.yml"
      provides: "Container orchestration"
    - path: "docker/entrypoint.sh"
      provides: "Container startup script"
  key_links:
    - from: "src/bitrag/cli/main.py"
      to: "click library"
      via: "import click"
    - from: "docker/docker-compose.yml"
      to: "docker/Dockerfile"
      via: "build context"
---

<objective>
Create complete project foundation: Python package structure, CLI skeleton, and Docker setup from day one.
</objective>

<execution_context>
@/home/itsb2sinha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/itsb2sinha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create project directory structure</name>
  <files>
    - src/bitrag/__init__.py
    - src/bitrag/cli/__init__.py
    - src/bitrag/core/__init__.py
    - src/bitrag/utils/__init__.py
    - tests/
    - data/
    - chroma_db/
    - docker/
    - scripts/
  </files>
  <action>
    Create the following directory structure:
    - src/bitrag/ - main package
    - src/bitrag/cli/ - CLI commands
    - src/bitrag/core/ - RAG engine
    - src/bitrag/utils/ - utilities
    - tests/ - test files
    - data/ - PDF storage (mount from host)
    - chroma_db/ - vector database (mount from host)
    - docker/ - Docker configs
    - scripts/ - Helper scripts
    
    Initialize each directory with __init__.py for proper Python packaging.
  </action>
  <verify>
    ls -la src/bitrag/ && ls -la src/bitrag/cli/
  </verify>
  <done>
    All directories created with __init__.py files
  </done>
</task>

<task type="auto">
  <name>Set up virtual environment</name>
  <files>
    - .venv/
  </files>
  <action>
    Create virtual environment using Python's built-in venv:
    
    Linux/Mac:
    ```bash
    python -m venv .venv
    source .venv/bin/activate
    ```
    
    Windows:
    ```bash
    python -m venv .venv
    .venv\Scripts\activate
    ```
    
    Then install dependencies:
    ```bash
    pip install -r requirements.txt
    pip install -e .
    ```
  </action>
  <verify>
    ls -la .venv/bin/ | head -5
  </verify>
  <done>
    Virtual environment created and dependencies installed
  </done>
</task>

<task type="auto">
  <name>Create requirements.txt with dependencies</name>
  <files>
    - requirements.txt
  </files>
  <action>
    Create requirements.txt with the following core dependencies:
    - llama-index>=0.10.0 - RAG framework
    - llama-index-vector-stores-chroma - ChromaDB integration
    - llama-index-embeddings-huggingface - Local embeddings
    - llama-index-llms-ollama - Ollama integration
    - chromadb - Vector database
    - sentence-transformers - Embedding models
    - pypdf - PDF parsing
    - click - CLI framework
    - python-dotenv - Environment variables
    - tqdm - Progress bars
    - colorama - Colored output
    - huggingface-hub - For downloading BitNet GGUF models
    
    Use version constraints that are known to work together.
  </action>
  <verify>
    .venv/bin/pip list | grep llama-index
  </verify>
  <done>
    requirements.txt created with all dependencies
  </done>
</task>

<task type="auto">
  <name>Create CLI entry point with basic commands</name>
  <files>
    - src/bitrag/cli/main.py
  </files>
  <action>
    Create CLI using Click framework with the following structure:
    
    1. Main group: `bitrag`
    2. Commands:
       - `upload <path>` - Upload and index PDF
       - `list` - List indexed documents
       - `delete <doc_id>` - Delete document
       - `query <text>` - Query documents
       - `chat` - Interactive chat mode
       - `model list` - List available Ollama models
       - `model pull <name>` - Pull Ollama model
       - `model use <name>` - Switch between BitNet/Ollama models
       - `model status` - Show current model
       - `status` - Show system status
    
    Each command should have basic help text and show a "Not implemented yet" message for now.
    
    Note: Include support for both BitNet (true 1-bit) and regular Ollama models.
  </action>
  <verify>
    python -m bitrag.cli.main --help
  </verify>
  <done>
    CLI runs and shows help with all commands listed
  </done>
</task>

<task type="auto">
  <name>Create pyproject.toml for package installation</name>
  <files>
    - pyproject.toml
  </files>
  <action>
    Create pyproject.toml with:
    - Project name: bitrag
    - Version: 0.1.0
    - Description: RAG with 1-bit LLM
    - Python >=3.10
    - Package configuration using setuptools
    - Entry points for CLI (bitrag command)
    - Read requirements.txt for dependencies
    
    Note: This is for pip install -e in virtual environment.
  </action>
  <verify>
    .venv/bin/pip install -e . && .venv/bin/bitrag --help
  </verify>
  <done>
    Package installs and CLI runs from anywhere
  </done>
</task>

<task type="auto">
  <name>Create Dockerfile for application</name>
  <files>
    - docker/Dockerfile
  </files>
  <action>
    Create Dockerfile with:
    - Base image: python:3.11-slim
    - Install system dependencies (gcc, libgomp1 for sentence-transformers)
    - Set working directory: /app
    - Copy requirements.txt first (for caching)
    - Install Python dependencies using pip
    - Copy application code (src/ and pyproject.toml)
    - Install package in editable mode
    - Set entrypoint
    
    Note: Virtual environment is for LOCAL development. Docker uses standard pip for smaller image.
  </action>
  <verify>
    docker build -f docker/Dockerfile -t bitrag:test . --no-cache
  </verify>
  <done>
    Dockerfile builds successfully
  </done>
</task>

<task type="auto">
  <name>Create Docker Compose with all services</name>
  <files>
    - docker/docker-compose.yml
  </files>
  <action>
    Create docker-compose.yml with:
    1. ollama service - LLM runtime
       - Image: ollama/ollama:latest
       - Ports: 11434:11434
       - Volumes: ollama_models for model cache
       - Restart: unless-stopped
       - Environment: OLLAMA_HOST=0.0.0.0
    
    2. app service - Python CLI
       - Build: . (from Dockerfile)
       - Volumes: 
         - ./data:/app/data - PDF storage
         - ./chroma_db:/app/chroma_db - Vector DB
       - Environment:
         - OLLAMA_BASE_URL=http://ollama:11434
         - DATA_DIR=/app/data
         - CHROMA_DIR=/app/chroma_db
         - DEFAULT_LLM_MODEL=bitnet
       - Depends on: ollama
       - Restart: unless-stopped
    
    Add healthcheck for Ollama to ensure it's ready before app runs.
    
    Note: App will support BOTH BitNet (true 1-bit) and regular Ollama models.
  </action>
  <verify>
    docker-compose -f docker/docker-compose.yml config
  </verify>
  <done>
    Docker Compose validates without errors
  </done>
</task>

<task type="auto">
  <name>Create entrypoint script</name>
  <files>
    - docker/entrypoint.sh
  </files>
  <action>
    Create entrypoint.sh that:
    - Waits for Ollama to be ready (curl health endpoint)
    - Shows startup message
    - Executes the passed CLI command
    
    Include:
    - Error handling for Ollama not ready
    - Retry logic for health check
    - Graceful exit on failure
  </action>
  <verify>
    cat docker/entrypoint.sh | head -20
  </verify>
  <done>
    Entrypoint script is executable and functional
  </done>
</task>

<task type="auto">
  <name>Create .dockerignore file</name>
  <files>
    - .dockerignore
  </files>
  <action>
    Create .dockerignore to exclude:
    - __pycache__
    - *.pyc
    - .git
    - .planning
    - tests/
    - README.md
    - *.md (except needed)
    - venv/
    - .env
  </action>
  <verify>
    cat .dockerignore
  </verify>
  <done>
    .dockerignore created with proper exclusions
  </done>
</task>

<task type="auto">
  <name>Test Docker Compose builds and starts</name>
  <files>
    - docker/docker-compose.yml
  </files>
  <action>
    Build and start the Docker Compose services:
    - docker-compose build --no-cache
    - docker-compose up -d
    - Check logs: docker-compose logs
    - Verify Ollama is accessible
    - Verify app container is running
    
    This validates the entire Docker setup works.
  </action>
  <verify>
    docker-compose ps && curl -s http://localhost:11434/api/tags
  </verify>
  <done>
    Docker Compose starts successfully with all services
  </done>
</task>

<task type="auto">
  <name>Create Model Downloader Script</name>
  <files>
    - scripts/download_model.py
  </files>
  <action>
    Create scripts/download_model.py with:
    
    1. Support for BOTH model types:
       - Ollama models: `ollama pull <model>`
       - BitNet GGUF: Download from HuggingFace using huggingface_hub
    
    2. Command line options:
       - `--type ollama` - Download Ollama model
       - --type bitnet` - Download BitNet GGUF from HuggingFace
       - `--model <name>` - Model name
       - `--list` - List available models
    
    3. Pre-configured models:
       - Ollama: llama3.2:1b, phi3:3.8b, qwen2:0.5b
       - BitNet: microsoft/bitnet-b1.58-2B-4T-gguf
    
    Usage:
    ```bash
    python scripts/download_model.py --type ollama --model llama3.2:1b
    python scripts/download_model.py --type bitnet
    python scripts/download_model.py --list
    ```
  </action>
  <verify>
    python scripts/download_model.py --help
  </verify>
  <done>
    Model downloader works for both Ollama and BitNet
  </done>
</task>

</tasks>

<verification>
- [ ] Project structure created
- [ ] Virtual environment created (.venv)
- [ ] requirements.txt created with all dependencies
- [ ] CLI runs with --help showing all commands
- [ ] Package installs via pip install -e .
- [ ] Dockerfile builds successfully
- [ ] Docker Compose validates and starts
- [ ] Ollama service is accessible
- [ ] App service runs and responds
</verification>

<success_criteria>
- Project has proper Python package structure
- Virtual environment ".venv" is created
- All dependencies listed in requirements.txt
- CLI is runnable and shows help
- Package installs correctly in virtual env
- Docker builds and runs successfully
- Docker Compose orchestrates all services
- Data persists in mounted volumes
</success_criteria>

<output>
After completion, create `.planning/phases/02-setup/{phase}-01-SUMMARY.md`
</output>
