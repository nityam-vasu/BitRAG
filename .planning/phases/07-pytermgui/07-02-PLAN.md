---
phase: 07-pytermgui
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Chat widget displays messages correctly"
    - "Input widget captures user text"
    - "Sidebar shows session/conversation list"
    - "Widgets integrate with main application"
  artifacts:
    - path: "src/bitrag/tui/widgets.py"
      provides: "Custom TUI widgets (chat, input, sidebar)"
  key_links: []
---

<objective>
Create core TUI widgets: message display, input field, and sidebar.

Purpose: Build reusable UI components for the terminal interface
Output: Working widgets that can be composed into the main UI
</objective>

<execution_context>
@/home/itsb2sinha/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/bitrag/tui/main.py
</context>

<tasks>

<task type="auto">
  <name>Create message/chat widget</name>
  <files>src/bitrag/tui/widgets.py</files>
  <action>
    Create ChatDisplay widget using PyTermGUI that:
    - Displays messages in a scrollable area
    - Shows user messages (right-aligned, different color)
    - Shows assistant messages (left-aligned)
    - Includes timestamps
    - Supports auto-scroll to bottom
  </action>
  <verify>python -c "from bitrag.tui.widgets import ChatDisplay; print('OK')"</verify>
  <done>ChatDisplay widget works</done>
</task>

<task type="auto">
  <name>Create input widget</name>
  <files>src/bitrag/tui/widgets.py</files>
  <action>
    Create InputWidget using PyTermGUI that:
    - Single-line text input
    - Enter key triggers submit
    - Escape key clears or cancels
    - Shows prompt like "You: "
  </action>
  <verify>python -c "from bitrag.tui.widgets import InputWidget; print('OK')"</verify>
  <done>InputWidget works</done>
</task>

<task type="auto">
  <name>Create sidebar widget</name>
  <files>src/bitrag/tui/widgets.py</files>
  <action>
    Create Sidebar widget using PyTermGUI that:
    - Lists sessions/conversations
    - Shows active session highlight
    - Click to switch sessions
    - Add/delete session buttons
  </action>
  <verify>python -c "from bitrag.tui.widgets import Sidebar; print('OK')"</verify>
  <done>Sidebar widget works</done>
</task>

</tasks>

<verification>
- [ ] All widgets import successfully
- [ ] Widgets render without errors
- [ ] Widgets respond to input
</verification>

<success_criteria>
Core TUI widgets created and functional
</success_criteria>

<output>
After completion, create `.planning/phases/07-pytermgui/07-02-SUMMARY.md`
</output>
