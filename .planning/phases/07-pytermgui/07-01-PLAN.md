---
phase: 07-pytermgui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "PyTermGUI is installed as dependency"
    - "Project structure supports PyTermGUI modules"
    - "Basic TUI skeleton runs without errors"
  artifacts:
    - path: "src/bitrag/tui/__init__.py"
      provides: "TUI package initialization"
    - path: "src/bitrag/tui/main.py"
      provides: "Main PyTermGUI application entry"
  key_links: []
---

<objective>
Set up PyTermGUI dependencies and create initial TUI application structure.

Purpose: Create foundation for terminal-based UI that wraps existing CLI functionality
Output: Running PyTermGUI application skeleton
</objective>

<execution_context>
@/home/itsb2sinha/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/bitrag/cli/main.py
@src/bitrag/core/indexer.py
@src/bitrag/core/query.py
@src/bitrag/core/config.py
</context>

<tasks>

<task type="auto">
  <name>Add PyTermGUI to requirements</name>
  <files>requirements.txt</files>
  <action>
    Add PyTermGUI to requirements.txt. Check latest version on PyPI. Add version constraint.
    Also add 'pyyaml' if not already present for configuration.
  </action>
  <verify>grep -q "pytermgui" requirements.txt && pip install -q pytermgui</verify>
  <done>PyTermGUI installed successfully</done>
</task>

<task type="auto">
  <name>Create TUI package structure</name>
  <files>src/bitrag/tui/__init__.py</files>
  <action>
    Create src/bitrag/tui/ directory with __init__.py. Export main application class.
  </action>
  <verify>ls -la src/bitrag/tui/</verify>
  <done>TUI package created</done>
</task>

<task type="auto">
  <name>Create main PyTermGUI application</name>
  <files>src/bitrag/tui/main.py</files>
  <action>
    Create basic PyTermGUI application that:
    - Imports existing CLI handlers (upload, query, chat, etc.)
    - Creates main application window with sidebar
    - Has basic layout structure
    - Currently shows placeholder content
    
    Reference existing CLI: src/bitrag/cli/main.py
  </action>
  <verify>python -c "from bitrag.tui.main import BitRAGApplication; print('OK')"</verify>
  <done>Basic PyTermGUI app runs without errors</done>
</task>

</tasks>

<verification>
 [ ] Py-TermGUI imports successfully
- [ ] Basic app window appears
- [ ] No import errors from existing CLI modules
</verification>

<success_criteria>
PyTermGUI setup complete, basic app runs
</success_criteria>

<output>
After completion, create `.planning/phases/07-pytermgui/07-01-SUMMARY.md`
</output>
