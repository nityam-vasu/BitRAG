---
phase: 07-pytermgui
plan: 06
type: execute
wave: 6
depends_on: [07-05]
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can change LLM model"
    - "User can configure hybrid search alpha"
    - "User can view/change data directories"
    - "Settings persist across restarts"
  artifacts:
    - path: "src/bitrag/tui/settings.py"
      provides: "Settings dialog for TUI"
  key_links: []
---

<objective>
Implement settings and configuration UI.

Purpose: Allow users to customize application behavior
Output: Settings dialog in terminal UI
</objective>

<execution_context>
@/home/itsb2sinha/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/bitrag/core/config.py
@src/bitrag/tui/widgets.py
</context>

<tasks>

<task type="auto">
  <name>Create settings dialog</name>
  <files>src/bitrag/tui/settings.py</files>
  <action>
    Create settings UI that:
    - Lists current configuration (model, embedding, directories)
    - Allows model selection (from available Ollama models)
    - Allows alpha parameter adjustment (0-1 slider or input)
    - Shows current directories with links
    - Save/cancel buttons
  </action>
  <verify>python -c "from bitrag.tui.settings import SettingsDialog; print('OK')"</verify>
  <done>SettingsDialog works</done>
</task>

<task type="auto">
  <name>Add settings to main window</name>
  <files>src/bitrag/tui/main.py</files>
  <action>
    Update main.py to:
    - Add settings button/menu item
    - Open settings dialog on trigger
    - Apply settings changes immediately
    - Show confirmation on save
  </action>
  <verify>python -c "from bitrag.tui.main import BitRAGApplication; print('OK')"</verify>
  <done>Settings integrated</done>
</task>

</tasks>

<verification>
- [ ] Settings dialog opens
- [ ] Can change model
- [ ] Can adjust alpha
- [ ] Settings persist after restart
</verification>

<success_criteria>
Settings fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/07-pytermgui/07-06-SUMMARY.md`
</output>
