---
phase: 07-pytermgui
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Hybrid search combines vector and keyword search"
    - "Document retrieval returns relevant chunks"
    - "Search is configurable via alpha parameter"
  artifacts:
    - path: "src/bitrag/tui/hybrid_search.py"
      provides: "Hybrid RAG integration for TUI"
  key_links: []
---

<objective>
Integrate Hybrid RAG (Vector + BM25) with the PyTermGUI application.

Purpose: Enable document search and retrieval within the terminal UI
Output: Working hybrid search accessible from TUI
</objective>

<execution_context>
@/home/itsb2sinha/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/bitrag/core/query.py
@src/bitrag/tui/widgets.py
</context>

<tasks>

<task type="auto">
  <name>Create TUI hybrid search handler</name>
  <files>src/bitrag/tui/hybrid_search.py</files>
  <action>
    Create hybrid search integration for TUI that:
    - Imports existing HybridSearch from Phase 06
    - Wraps query engine for use in TUI context
    - Provides async-friendly interface
    - Shows loading indicator during search
    - Displays source citations
  </action>
  <verify>python -c "from bitrag.tui.hybrid_search import TUIQueryEngine; print('OK')"</verify>
  <done>TUIQueryEngine works</done>
</task>

<task type="auto">
  <name>Integrate search with widgets</name>
  <files>src/bitrag/tui/main.py</files>
  <action>
    Update main.py to:
    - Initialize TUIQueryEngine on startup
    - Connect input widget to search
    - Display results in chat widget
    - Handle streaming responses
  </action>
  <verify>python -c "from bitrag.tui.main import BitRAGApplication; print('OK')"</verify>
  <done>Search integration works</done>
</task>

</tasks>

<verification>
- [ ] Hybrid search returns results
- [ ] Results display in chat area
- [ ] Sources are shown
</verification>

<success_criteria>
Hybrid RAG integrated with TUI
</success_criteria>

<output>
After completion, create `.planning/phases/07-pytermgui/07-03-SUMMARY.md`
</output>
