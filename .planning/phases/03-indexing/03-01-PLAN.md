---
phase: 03-indexing
plan: 01
type: execute
wave: 1
depends_on: [02-01]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Session system works (create/activate)"
    - "PDF files can be uploaded and parsed"
    - "Documents are indexed and stored in ChromaDB"
    - "Can list and delete indexed documents"
    - "Progress bar shows indexing progress"
  artifacts:
    - path: "src/bitrag/core/indexer.py"
      provides: "Document indexing logic"
    - path: "src/bitrag/core/config.py"
      provides: "Configuration management"
    - path: "scripts/create_session.py"
      provides: "Session directory creator"
    - path: "scripts/activate_session.py"
      provides: "Upload + Index with progress bar"
  key_links:
    - from: "scripts/create_session.py"
      to: "src/bitrag/core/config.py"
      via: "session storage"
    - from: "scripts/activate_session.py"
      to: "llama_index"
      via: "indexing"
---

<objective>
Implement PDF upload, parsing, and vector indexing using LlamaIndex with ChromaDB.
</objective>

<execution_context>
@/home/itsb2sinha/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-setup/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create Session Creator Script</name>
  <files>
    - scripts/create_session.py
  </files>
  <action>
    Create scripts/create_session.py with:
    
    1. Functionality:
       - Create a unique session directory under sessions/
       - Session ID: timestamp-based or user-provided name
       - Each session has: uploads/, index/, chroma_db/
    
    2. Command line options:
       - `--name <session_name>` - Optional custom name
       - `--list` - List all sessions
       - `--delete <session_id>` - Delete a session
    
    3. Directory structure per session:
       ```
       sessions/<session_id>/
       ├── uploads/     # User uploaded PDFs
       ├── index/       # Index files
       └── chroma_db/  # Vector database
       ```
    
    Usage:
    ```bash
    python scripts/create_session.py
    python scripts/create_session.py --name my_project
    python scripts/create_session.py --list
    ```
  </action>
  <verify>
    python scripts/create_session.py --help
  </verify>
  <done>
    Session creator works correctly
  </done>
</task>

<task type="auto">
  <name>Create Session Activator + Upload + Indexing Script</name>
  <files>
    - scripts/activate_session.py
  </files>
  <action>
    Create scripts/activate_session.py with:
    
    1. Functionality:
       - Activate session by ID (authentication)
       - Upload PDF files to session
       - Index uploaded files with REAL-TIME progress bar
    
    2. Command line options:
       - `--session <session_id>` - Session ID to activate
       - `--upload <pdf_path>` - PDF file to upload
       - `--index` - Start indexing after upload
       - `--status` - Show session status
    
    3. Progress bar (using tqdm):
       - Phase 1: Loading PDF... [==========]
       - Phase 2: Extracting text... [==========]
       - Phase 3: Chunking... [==========]
       - Phase 4: Creating embeddings... [==========]
       - Phase 5: Storing in vector DB... [==========]
    
    Usage:
    ```bash
    python scripts/activate_session.py --session 20250213_143022
    python scripts/activate_session.py --session 20250213_143022 --upload document.pdf --index
    ```
  </action>
  <verify>
    python scripts/activate_session.py --help
  </verify>
  <done>
    Session activation + upload + indexing works with progress bar
  </done>
</task>

<task type="auto">
  <name>Create configuration module</name>
  <files>
    - src/bitrag/core/config.py
  </files>
  <action>
    Create config.py with:
    - Data directory path (configurable)
    - ChromaDB persist directory
    - Embedding model configuration
    - Default chunk size and overlap
    - Ollama base URL
    - Collection name for documents
    - **LLM Model Selection** (new):
      - default_model: str = "bitnet" (or "llama3.2:1b", etc.)
      - Model type: "bitnet" | "ollama"
      - Available BitNet models list
      - Available Ollama models list
    
    Use dataclasses or Pydantic for type safety.
  </action>
  <verify>
    python -c "from bitrag.core.config import Config; c = Config(); print(c.default_model)"
  </verify>
  <done>
    Configuration module loads without errors
  </done>
</task>

<task type="auto">
  <name>Create document indexer module</name>
  <files>
    - src/bitrag/core/indexer.py
  </files>
  <action>
    Create indexer.py with:
    1. PDF loading using PyPDFReader or SimpleDirectoryReader
    2. Text chunking using SentenceSplitter
    3. Embedding setup (all-MiniLM-L6-v2)
    4. ChromaDB vector store with PersistentClient
    5. Index creation from documents
    
    Implement:
    - `index_document(path: str) -> str` - Index a PDF, return doc_id
    - `list_documents() -> list` - List all indexed docs
    - `delete_document(doc_id: str)` - Remove from index
    - `get_document_count() -> int` - Count indexed docs
  </action>
  <verify>
    python -c "from bitrag.core.indexer import DocumentIndexer; print('Indexer loaded')"
  </verify>
  <done>
    Indexer module loads and has all required methods
  </done>
</task>

<task type="auto">
  <name>Connect CLI upload command to indexer</name>
  <files>
    - src/bitrag/cli/commands.py
  </files>
  <action>
    Update CLI commands to:
    - `upload` command - Calls DocumentIndexer.index_document()
    - `list` command - Shows all indexed documents with metadata
    - `delete` command - Removes document from index
    - Add progress feedback during indexing
  </action>
  <verify>
    bitrag upload --help && bitrag list --help
  </verify>
  <done>
    Upload, list, delete commands work with indexer
  </done>
</task>

<task type="auto">
  <name>Test PDF indexing end-to-end</name>
  <files>
    - tests/test_indexing.py
  </files>
  <action>
    Create test file that:
    1. Downloads a sample PDF (or uses existing in data/)
    2. Indexes the PDF
    3. Verifies document count increases
    4. Lists documents
    5. Deletes the document
    6. Verifies count decreases
  </action>
  <verify>
    pytest tests/test_indexing.py -v
  </verify>
  <done>
    Full indexing workflow works correctly
  </done>
</task>

</tasks>

<verification>
- [ ] Configuration loads correctly
- [ ] PDF files can be indexed
- [ ] Documents persist in ChromaDB
- [ ] List command shows indexed documents
- [ ] Delete removes documents from index
</verification>

<success_criteria>
- PDF upload indexes successfully
- Documents are retrievable from ChromaDB
- List shows all indexed documents with metadata
- Delete removes document completely
</success_criteria>

<output>
After completion, create `.planning/phases/03-indexing/{phase}-01-SUMMARY.md`
</output>
