---
phase: 05
plan: 01
type: execute
wave: 1
depends_on: [04-01]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Ollama works for RAG pipeline"
    - "CLI has opencode-style commands"
    - "Smaller LLMs available (llama3.2:1b, qwen2.5:0.5b)"
  artifacts:
    - path: "src/bitrag/cli/main.py"
      provides: "opencode-style CLI"
---

<objective>
Enhance CLI to opencode-style and use Ollama with smaller LLMs (no high GPU required).
</objective>

<execution_context>
@/home/itsb2sinha/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-query/04-01-SUMMARY.md
@.planning/phases/04-bitnet/04-01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Test smaller Ollama models</name>
  <files>
    - None
  </files>
  <action>
    Pull and test smaller Ollama models:
    
    1. ollama pull llama3.2:1b
    2. ollama pull qwen2.5:0.5b
    3. ollama list (verify)
    4. Test query with smaller model
    
    These models work without high GPU requirements.
  </action>
  <verify>
    ollama list | grep -E "llama3.2|qwen2.5"
  </verify>
  <done>
    Smaller models available in Ollama
  </done>
</task>

<task type="auto">
  <name>Update CLI to opencode-style</name>
  <files>
    - src/bitrag/cli/main.py
  </files>
  <action>
    Transform CLI to opencode-style:
    
    1. Keep Click but add:
       - Slash command support: /upload, /query, /chat
       - Interactive prompt mode
       - Rich output with colors
       - Command groups
    
    2. Main commands:
       - upload <file>
       - query <text>
       - chat
       - model [list|use|status]
       - session [list|create|activate]
    
    3. Help improvements:
       - Show available commands
       - Command descriptions
    
    Example:
    bitrag /upload data/doc.pdf
    bitrag /query "What is this about?"
    bitrag /chat
  </action>
  <verify>
    python -m bitrag.cli.main --help
  </verify>
  <done>
    CLI has opencode-style commands
  </done>
</task>

<task type="auto">
  <name>Update config for smaller models</name>
  <files>
    - src/bitrag/core/config.py
  </files>
  <action>
    Update default model to smaller one:
    
    1. Change default_model to llama3.2:1b
    2. Add list of recommended smaller models
    3. Document GPU requirements
    
    New defaults:
    - default_model: "llama3.2:1b"
    - llm_type: "ollama"
  </action>
  <verify>
    python -c "from bitrag.core.config import get_config; c=get_config(); print(c.default_model)"
  </verify>
  <done>
    Config uses smaller model by default
  </done>
</task>

<task type="auto">
  <name>Test RAG with smaller model</name>
  <files>
    - None
  </files>
  <action>
    Test full RAG pipeline with smaller model:
    
    1. Ensure document is indexed
    2. Query with llama3.2:1b
    3. Verify response is generated
    4. Compare quality vs gemma3:4b
  </action>
  <verify>
    python -m bitrag.cli.main query "What is BitRAG?" --session test --model llama3.2:1b
  </verify>
  <done>
    RAG works with smaller model
  </done>
</task>

</tasks>

<verification>
- [ ] Smaller models available (llama3.2:1b, qwen2.5:0.5b)
- [ ] CLI has /command style
- [ ] Config uses smaller model by default
- [ ] RAG pipeline works with smaller model
</verification>

<success_criteria>
- CLI follows opencode style
- RAG works with smaller LLMs (no high GPU needed)
- Options open for BitNet.cpp in future
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli/{phase}-01-SUMMARY.md`
</output>
