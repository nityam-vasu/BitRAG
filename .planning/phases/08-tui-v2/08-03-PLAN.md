---
phase: 08-tui-v2
plan: 03
type: execute
wave: 3
depends_on: [08-01]
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Settings page accessible via settings button"
    - "Ollama port configurable"
    - "Model selection from available Ollama models works"
    - "Model download with progress indicator works"
    - "Model delete functionality works"
    - "Dual model mode toggle with disclaimer works"
    - "Hybrid retrieval slider (vector/keyword balance) works"
    - "Document management button navigates to document page"
  artifacts:
    - path: "src/bitrag/tui/settings.py"
      provides: "Settings dialog and configuration UI"
  key_links: []
---

<objective>
Implement Settings page with model management and hybrid search configuration.

Purpose: Allow users to customize application behavior from TUI
Output: Full settings functionality in terminal UI
</objective>

<execution_context>
@/home/itsb2sinha/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/bitrag/tui/app.py
@src/bitrag/core/config.py
</context>

<tasks>

<task type="auto">
  <name>Create settings page</name>
  <files>src/bitrag/tui/settings.py</files>
  <action>
    Create src/bitrag/tui/settings.py with:
    - Ollama Port: Input field for port configuration (default: 11434)
    - Model Selection: Dropdown/list of available Ollama models
    - Model Download: Dialog with predefined list + custom input, progress bar
    - Model Delete: Dialog showing installed models with delete button
    - Dual Model Mode: Toggle with disclaimer "Using two models increases inference time and resource usage", shows Model 1/Model 2 inputs when enabled
    - Hybrid Retrieval Slider: Three-point slider (-1: Pure Vector, 0: Hybrid, 1: Pure Keyword)
    - Document Management Button: Navigates to document management page
    
    All settings should persist via existing config system
  </action>
  <verify>python -c "from bitrag.tui.settings import SettingsDialog; print('OK')"</verify>
  <done>Settings page created</done>
</task>

<task type="auto">
  <name>Integrate settings with main app</name>
  <files>src/bitrag/tui/app.py</files>
  <action>
    Update app.py:
    - Add Settings button in header (top right)
    - Open settings dialog on button click
    - Apply settings changes immediately
    - Show confirmation on save
    - Settings accessible via 'S' keyboard shortcut
  </action>
  <verify>python -c "from bitrag.tui.app import BitRAGApp; print('OK')"</verify>
  <done>Settings integrated with main app</done>
</task>

</tasks>

<verification>
- [ ] Settings page accessible
- [ ] Can configure Ollama port
- [ ] Can select model
- [ ] Can download model with progress
- [ ] Can delete model
- [ ] Dual model mode toggle works
- [ ] Hybrid slider works
- [ ] Document management button navigates
</verification>

<success_criteria>
Settings page fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/08-tui-v2/08-03-SUMMARY.md`
</output>