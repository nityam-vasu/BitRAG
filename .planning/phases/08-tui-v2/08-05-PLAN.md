---
phase: 08-tui-v2
plan: 05
type: execute
wave: 5
depends_on: [08-02, 08-03, 08-04]
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Application starts without errors"
    - "Keyboard shortcuts work (C, S, U, Q)"
    - "Mouse navigation works"
    - "Error handling is robust"
    - "System resources display in header"
  artifacts:
    - path: "src/bitrag/tui/__main__.py"
      provides: "Application entry point"
  key_links: []
---

<objective>
Implement keyboard/mouse navigation, system resource display, and final polish.

Purpose: Complete the TUI with navigation and polish
Output: Production-ready PyTermGUI application
</objective>

<execution_context>
@/home/itsb2sinha/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/bitrag/tui/app.py
</context>

<tasks>

<task type="auto">
  <name>Implement keyboard shortcuts</name>
  <files>src/bitrag/tui/app.py</files>
  <action>
    Add keyboard shortcuts:
    - C: Chat screen (main window)
    - S: Settings
    - U: Upload Files (document management)
    - Q: Quit
    
    Also implement mouse click navigation for buttons
  </action>
  <verify>Run app, test each shortcut</verify>
  <done>Keyboard shortcuts work</done>
</task>

<task type="auto">
  <name>Add system resource display</name>
  <files>src/bitrag/tui/app.py</files>
  <action>
    Add header area showing:
    - CPU usage (e.g., "CPU 5%")
    - Memory usage (e.g., "Memory 2GB / 16GB")
    - GPU usage (e.g., "GPU 56%")
    
    Update resource display periodically
  </action>
  <verify>python -c "from bitrag.tui.app import BitRAGApp; print('OK')"</verify>
  <done>Resource display works</done>
</task>

<task type="auto">
  <name>Add footer with shortcuts reference</name>
  <files>src/bitrag/tui/app.py</files>
  <action>
    Add footer area showing:
    ```
    C -→ Chat
    S -→ Settings
    U -→ Upload Files
    Q -→ Quit
    ```
  </action>
  <verify>python -c "from bitrag.tui.app import BitRAGApp; print('OK')"</verify>
  <done>Footer shortcuts displayed</done>
</task>

<task type="auto">
  <name>Improve error handling</name>
  <files>src/bitrag/tui/app.py</files>
  <action>
    Improve error handling:
    - Catch Ollama connection errors with user-friendly message
    - Handle missing documents gracefully
    - Show appropriate error dialogs
    - Log errors for debugging
  </action>
  <verify>Test error scenarios</verify>
  <done>Errors handled gracefully</done>
</task>

<task type="auto">
  <name>Create entry point</name>
  <files>src/bitrag/tui/__main__.py</files>
  <action>
    Create src/bitrag/tui/__main__.py that:
    - Provides `python -m bitrag.tui` entry point
    - Shows splash screen
    - Launches main app
  </action>
  <verify>python -m bitrag.tui --help</verify>
  <done>Entry point works</done>
</task>

</tasks>

<verification>
- [ ] Keyboard shortcuts work (C, S, U, Q)
- [ ] Mouse navigation works
- [ ] System resources display in header
- [ ] Footer shows shortcuts
- [ ] Error handling is robust
- [ ] Entry point works
</verification>

<success_criteria>
Application production-ready with full navigation
</success_criteria>

<output>
After completion, create `.planning/phases/08-tui-v2/08-05-SUMMARY.md`
</output>