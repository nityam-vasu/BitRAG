---
phase: 08-tui-v2
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can type and send queries"
    - "Thinking box shows reasoning when model supports it"
    - "Model output displays with markdown rendering"
    - "Sources button shows referenced documents"
    - "Streaming responses display progressively"
  artifacts:
    - path: "src/bitrag/tui/chat_display.py"
      provides: "Chat message display with thinking/output/sources"
  key_links: []
---

<objective>
Implement chat interface with thinking box, model output, and sources display.

Purpose: Enable conversational interaction with RAG system in the TUI
Output: Full chat functionality with streaming and source citations
</objective>

<execution_context>
@/home/itsb2sinha/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/bitrag/tui/app.py
@src/bitrag/core/query.py
</context>

<tasks>

<task type="auto">
  <name>Create chat message display components</name>
  <files>src/bitrag/tui/chat_display.py</files>
  <action>
    Create src/bitrag/tui/chat_display.py with:
    - UserQueryWidget: Displays user query in a styled box (right-aligned)
    - ThinkingWidget: Collapsible box showing reasoning steps, supports markdown
    - ModelOutputWidget: Displays final response with markdown rendering
    - SourcesWidget: Button that opens dialog showing referenced documents
    
    All widgets should use PyTermGUI Window with appropriate styling
  </action>
  <verify>python -c "from bitrag.tui.chat_display import ThinkingWidget, ModelOutputWidget, SourcesWidget; print('OK')"</verify>
  <done>Chat message components created</done>
</task>

<task type="auto">
  <name>Implement streaming response display</name>
<files>src/bitrag/tui/app.py</files>
<action>
    Update app.py to:
    - Stream LLM responses character-by-character or word-by-word
    - Show Thinking widget during reasoning (if model supports it)
    - Progressively update ModelOutputWidget as response arrives
    - Handle streaming cancellation (Ctrl+C)
    - Show loading indicator during generation
    
    Integrate with existing query engine from src/bitrag/core/query.py
  </action>
  <verify>Start app, send query, see streaming response</verify>
  <done>Streaming responses work</done>
</task>

<task type="auto">
  <name>Implement chat input bar</name>
  <files>src/bitrag/tui/app.py</files>
  <action>
    Update chat bar in main window:
    - File Upload Button: Opens dialog to accept file paths (multiple files)
    - Query Input Field: Single-line text input for user queries
    - Send Button: Submits query to query engine
    
    Connect input to query engine and display results
  </action>
  <verify>python -c "from bitrag.tui.app import BitRAGApp; print('OK')"</verify>
  <done>Chat bar functional</done>
</task>

</tasks>

<verification>
- [ ] Can send queries
- [ ] Thinking box shows reasoning (if supported)
- [ ] Model output displays with markdown
- [ ] Sources button shows referenced documents
- [ ] Responses stream progressively
</verification>

<success_criteria>
Chat interface fully functional with streaming and sources
</success_criteria>

<output>
After completion, create `.planning/phases/08-tui-v2/08-02-SUMMARY.md`
</output>