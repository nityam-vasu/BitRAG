---
phase: 08-tui-v2
plan: 04
type: execute
wave: 4
depends_on: [08-01]
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can view list of indexed documents"
    - "User can upload documents with progress indicator"
    - "User can delete documents with confirmation"
    - "Document operations update the UI"
  artifacts:
    - path: "src/bitrag/tui/document_manager.py"
      provides: "Document management UI"
  key_links: []
---

<objective>
Implement Document Management page for uploading, listing, and deleting documents.

Purpose: Allow users to manage their document collection from the terminal
Output: Full document management in TUI
</objective>

<execution_context>
@/home/itsb2sinha/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/bitrag/tui/app.py
@src/bitrag/core/indexer.py
</context>

<tasks>

<task type="auto">
  <name>Create document management page</name>
  <files>src/bitrag/tui/document_manager.py</files>
  <action>
    Create src/bitrag/tui/document_manager.py with:
    - List Documents: Display all indexed documents (filename, chunks, date)
    - Upload Document: Dialog to enter file path, shows indexing progress bar
    - Delete Document: Dialog showing indexed documents with delete buttons, shows "Removing index..." progress, confirms deletion
    
    Integrate with existing DocumentIndexer from src/bitrag/core/indexer.py
  </action>
  <verify>python -c "from bitrag.tui.document_manager import DocumentManager; print('OK')"</verify>
  <done>Document manager created</done>
</task>

<task type="auto">
  <name>Integrate document management with main app</name>
  <files>src/bitrag/tui/app.py</files>
  <action>
    Update app.py:
    - Add Documents button/indicator in header
    - Add "U" keyboard shortcut to access document management
    - Show document count in header area
    - Access via settings page "Document Management Button"
  </action>
  <verify>python -c "from bitrag.tui.app import BitRAGApp; print('OK')"</verify>
  <done>Document management integrated</done>
</task>

</tasks>

<verification>
- [ ] Can view list of indexed documents
- [ ] Can upload documents with progress
- [ ] Can delete documents with confirmation
- [ ] UI updates after operations
</verification>

<success_criteria>
Document management fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/08-tui-v2/08-04-SUMMARY.md`
</output>