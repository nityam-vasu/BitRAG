---
phase: 04-query
plan: 01
type: execute
wave: 1
depends_on: [03-01]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Can query indexed documents"
    - "1-bit LLM (BitNet) generates responses"
    - "Streaming responses work"
    - "Session-based chat works"
  artifacts:
    - path: "src/bitrag/core/query.py"
      provides: "RAG query engine"
  key_links:
    - from: "src/bitrag/core/query.py"
      to: "src/bitrag/core/indexer.py"
      via: "uses index"
    - from: "src/bitrag/core/query.py"
      to: "ollama"
      via: "Ollama LLM"
    - from: "scripts/chat_session.py"
      to: "src/bitrag/core/query.py"
      via: "streaming chat"
---

<objective>
Implement the complete RAG pipeline: retrieve relevant chunks, generate response with 1-bit LLM.
</objective>

<execution_context>
@/home/itsb2sinha/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-indexing/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create RAG query engine with model selector</name>
  <files>
    - src/bitrag/core/query.py
  </files>
  <action>
    Create query.py with:
    1. Ollama LLM setup with model selector
    2. Support for BOTH model types:
       - DEFAULT: BitNet 1.58 (microsoft/bitnet-b1.58-2B-4T-gguf)
       - Alternative: Regular Ollama models (llama3.2:1b, phi3, etc.)
    3. Query engine creation using LlamaIndex
    4. Retrieval configuration (top_k=3-5)
    5. Prompt template for RAG
    
    Implement:
    - `QueryEngine` class with model selection
    - `set_model(model_name: str)` - Switch between BitNet/Ollama
    - `get_current_model()` - Show current model
    - `query(question: str) -> Response` - Single query
    - `query_streaming(question: str) -> Generator` - Streaming
    - `get_retrieved_context(question: str) -> list` - Get chunks without LLM
    
    Add config option for default model selection.
  </action>
  <verify>
    python -c "from bitrag.core.query import QueryEngine; print('QueryEngine loaded')"
  </verify>
  <done>
    Query engine loads with model selection support
  </done>
</task>

<task type="auto">
  <name>Connect CLI query commands to engine</name>
  <files>
    - src/bitrag/cli/commands.py
  </files>
  <action>
    Update CLI commands to:
    - `query <text>` - Single query with answer
    - `chat` - Interactive chat mode (continuous)
    - `stream <text>` - Streaming response
    
    Add:
    - Source citation display (which chunks were used)
    - Progress indicator for LLM generation
    - Error handling for no indexed documents
  </action>
  <verify>
    bitrag query --help && bitrag chat --help
  </verify>
  <done>
    Query commands work with RAG engine
  </done>
</task>

<task type="auto">
  <name>Test RAG query pipeline end-to-end</name>
  <files>
    - tests/test_query.py
  </files>
  <action>
    Create test that:
    1. Ensures at least one document is indexed
    2. Runs a query against indexed docs
    3. Verifies response is not empty
    4. Verifies context was retrieved (sources)
  </action>
  <verify>
    pytest tests/test_query.py -v
  </verify>
  <done>
    RAG query returns relevant answers
  </done>
</task>

</tasks>

<verification>
- [ ] Query engine loads and initializes Ollama
- [ ] Queries retrieve relevant context
- [ ] LLM generates coherent responses
- [ ] Streaming works correctly
</verification>

<success_criteria>
- User can ask questions about indexed PDFs
- 1-bit LLM generates contextually relevant answers
- Response shows sources/citations
- Chat mode allows continuous conversation
</success_criteria>

<output>
After completion, create `.planning/phases/04-query/{phase}-01-SUMMARY.md`
</output>
